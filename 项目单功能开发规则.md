请按照当前项目的项目文档和既有代码，继续进行**单个功能的开发与完善**。

## 一、需求依据

开发时必须以当前项目中的以下文件为主要依据：

```text
PROJECT.md
AGENTS.md
docs/ARCHITECTURE.md
docs/API.md
docs/DATABASE.md
docs/DEVELOPMENT.md
docs/TESTING.md

```

其中：

* `PROJECT.md`：项目目标、正式功能需求和开发范围
* `AGENTS.md`：代码修改和 AI 开发规则
* `docs/API.md`：现有 API
* `docs/DATABASE.md`：数据库结构
* `docs/ARCHITECTURE.md`：系统架构
* `docs/DEVELOPMENT.md`：开发环境
* `docs/TESTING.md`：测试方法

**实际源码优先于文档中对“当前实现状态”的描述。**

如果文档和源码不一致：

> 先检查源码，以实际代码为准，并在必要时同步修正文档。

---

# 二、当前项目范围

本项目正式课题为：

> 题目七：基于Web/微信小程序的校园失物招领系统设计与实现

但当前实际开发范围已经确定：

> **只开发 Web 端，不开发微信小程序。**

因此：

* 只修改 Web 前端
* 只修改 PHP 后端
* 只修改 Web API
* 必要时修改 MySQL 数据结构
* 使用浏览器进行 Web 测试
* 不创建微信小程序代码
* 不创建小程序页面
* 不创建小程序项目结构

---

# 三、核心开发原则

## 1. 一次只开发一个功能

当前任务只允许完成：

> **我本次指定的一个功能。**

不要顺便实现其他功能。

例如当前指定：

```text
功能6：认领申请

```

那么本次只能围绕“认领申请”进行开发。

不要同时实现：

* 认领审核
* 消息通知
* 管理员
* 其他无关功能

如果其他功能是当前功能的必要依赖，可以进行**最小必要修改**，但不能顺便把其他功能完整实现。

---

## 2. 修改之前先分析

不要看到需求就直接写代码。

开始前先检查：

```text
当前 Git 状态
相关前端页面
相关 JavaScript
相关 PHP API
数据库表结构
现有认证/Session
现有权限控制
相关文档
已有类似功能

```

然后简要整理：

```text
当前功能状态：
xxx

已经存在：
xxx

缺少：
xxx

本次需要修改：
xxx

预计涉及文件：
xxx

```

确认现有实现后再开始修改。

---

# 四、优先复用现有代码

当前项目已经存在大量功能。

必须优先：

> **在现有架构和代码基础上扩展。**

不要因为实现一个功能就重新创建一套系统。

例如已经存在：

```text
Session 登录
用户系统
消息系统
评论系统
列表系统
管理员系统

```

就应该复用。

禁止无必要地创建：

```text
第二套登录系统
第二套用户系统
第二套消息系统
第二套 API
第二套数据库结构

```

---

# 五、数据库修改规则

如果当前功能需要数据库支持：

先检查：

```text
DB_create.sql
docs/DATABASE.md
实际数据库结构

```

然后再决定是否需要修改。

原则：

1. 优先复用已有表
2. 优先复用已有字段
3. 确实无法满足需求时才增加字段/表
4. 修改 `DB_create.sql`
5. 同步更新 `docs/DATABASE.md`
6. 测试数据库操作

禁止：

* 随意删除已有字段
* 随意删除已有表
* 破坏已有数据
* 重建整个数据库
* 修改与当前功能无关的数据库结构

---

# 六、API 修改规则

如果需要修改 API：

先阅读实际 PHP 代码。

不要根据文件名猜测 API 行为。

需要新增 API 时：

* 遵循现有 API 目录结构
* 遵循现有认证方式
* 遵循现有错误处理方式
* 遵循现有 JSON 返回格式
* 遵循现有权限控制
* 防止未登录用户越权操作
* 防止用户操作其他用户的数据

如果 API 行为发生变化：

同步更新：

```text
docs/API.md

```

---

# 七、前端开发与测试规则

当前项目是 Web 项目。

修改前端后必须实际通过浏览器测试。

优先使用：

> Chrome DevTools MCP

检查：

```text
页面是否正常加载
JavaScript Console 是否报错
Network 请求是否成功
API 状态码
API 返回 JSON
登录状态
权限
表单提交
数据展示
错误提示

```

### ⚠️ 测试登录账号规定

在测试需要登录或鉴权的功能时：

* **严禁尝试走“用户注册”流程**（注册含有邮箱验证码校验，会导致自动化测试卡住或失败）。
* **直接使用既有测试账号登录**：
* **用户名**：`testuser`
* **密码**：`12345678`



不要只检查代码有没有语法错误。必须验证实际 Web 页面行为。

---

# 八、安全要求

涉及用户数据、认领申请、管理员操作等功能时，必须检查：

```text
登录状态
用户身份
数据所有权
管理员权限
SQL 注入
XSS
CSRF
文件上传
参数校验

```

尤其注意：

> 不能只在前端判断权限。

关键权限必须由后端 PHP API 再次验证。

例如用户 A 不能通过修改请求参数操作用户 B 的数据。

---

# 九、不要虚构功能

需求文档描述的是：

> **项目目标。**

不是：

> **当前已经实现的功能。**

如果当前功能实际上没有实现：

就按照需求进行实现。

如果发现需求涉及其他尚未实现的功能：

不要假装已经存在。

例如当前实现“认领申请”时发现“认领审核”尚未实现：

可以为认领申请设计合理的数据状态，使以后能够支持审核。

但是：

> **本次不要顺便把认领审核完整实现。**

---

# 十、功能完成标准

只有满足以下条件，才能认为本次功能完成。

### 代码

相关前后端代码已经实现。

### 数据库

如果需要数据库，结构和 CRUD 正常。

### Web 页面

用户可以通过实际 Web 页面完成操作。

### API

API 请求和返回正常。

### 权限

登录、普通用户、管理员等权限符合需求。

### 异常情况

至少测试：

```text
正常操作
未登录操作
无效参数
重复操作
无权限操作
不存在的数据

```

根据功能实际情况选择需要测试的项目。

### 文档

如果本次修改导致：

* API 变化
* 数据库变化
* 架构变化
* 测试方式变化

同步更新对应 MD。

不要无意义地修改其他文档。

---

# 十一、Git 提交规则

这是本项目非常重要的要求：

> **每完成一个功能，必须单独提交一次 Git。**

完成当前功能后：

先执行：

```powershell
git status
git diff --stat
git diff

```

确认：

* 没有无关文件
* 没有其他功能的修改
* 没有 `.env` 等敏感文件
* 修改内容确实属于当前功能

然后提交。

Commit 格式：

```text
<英文类型>: <中文描述>

```

例如：

```text
feat: 实现认领申请功能
feat: 完善失物信息发布功能
fix: 修复认领申请权限校验
docs: 更新认领申请接口文档

```

要求：

> **冒号后的描述必须使用中文。**

允许保留必要的英文技术关键词，例如：

```text
API
Web
PHP
MySQL
Session
HTML
JavaScript
CSS
Nginx

```

禁止使用纯英文描述：

```text
feat: implement claim feature
fix: fix permission bug
docs: update api

```

---

# 十二、一个功能可以修改多个文件

例如实现一个完整功能可能涉及：

```text
frontend/xxx.html
frontend/xxx.js
backend/api/xxx.php
DB_create.sql
docs/API.md
docs/DATABASE.md

```

这是允许的。

只要这些修改都是为了：

> **当前这一个功能。**

就可以放在同一个 commit。

---

# 十三、禁止为了 Git 拆分功能

不要把一个完整功能人为拆成：

```text
commit 1：创建数据库
commit 2：创建 API
commit 3：创建页面
commit 4：修改 JS

```

如果这些都是同一个功能的一部分：

> 应该作为一个完整功能提交。

例如：

```text
feat: 实现认领申请功能

```

一个 commit 完整包含这个功能的相关修改。

---

# 十四、完成后必须停止

这是强制要求。

完成当前功能后：

```text
代码修改
↓
测试
↓
文档同步
↓
git diff
↓
git commit
↓
git status
↓
汇报
↓
停止

```

**不要自动开始下一个功能。**

即使你认为下一个功能很简单，也必须等待我的下一条指令。

---

# 十五、完成后的汇报格式

完成后简要汇报：

```text
功能：XXX

实现内容：
- xxx
- xxx
- xxx

修改文件：
- xxx
- xxx
- xxx

测试：
- xxx
- xxx
- xxx

Git：
<commit hash> feat: 中文提交描述

工作区：
clean

```

如果有问题：

```text
功能：XXX

已完成：
xxx

未完成：
xxx

原因：
xxx

当前未提交修改：
xxx

```

不要隐瞒失败的测试。

---

# 十六、如果遇到问题

如果遇到：

* 需求不明确
* 数据库结构冲突
* 现有代码设计无法安全扩展
* API 行为存在重大冲突
* 需要改变其他功能
* 测试无法通过
* 需要我做产品决策

不要擅自做重大设计。

先停止并告诉我：

```text
当前功能：
xxx

问题：
xxx

现有实现：
xxx

建议方案：
xxx

需要确认：
xxx

```

---

# 十七、当前执行方式

当我发送：

```text
开始功能 N

```

或者：

```text
实现功能 N：XXX

```

你就只处理这个功能。

执行流程：

```text
1. git status
2. 阅读相关 MD
3. 阅读相关源码
4. 分析当前实现
5. 制定最小修改方案
6. 修改代码
7. 测试（测试登录时使用 testuser / 12345678）
8. 使用 Chrome DevTools MCP 验证 Web
9. 必要时更新 MD
10. git diff
11. git commit
12. git status
13. 汇报
14. 停止

```

---

# 十八、最终功能目标

最终需要完成 PROJECT.md 中定义的 10 项正式需求：

```text
1. 用户注册与登录
2. 失物信息发布
3. 招领信息发布
4. 信息查询
5. 信息匹配
6. 认领申请
7. 认领审核
8. 个人中心
9. 消息通知
10. 管理员功能

```

但是：

> **不要一次完成全部功能。**

严格按照：

> **一个功能 → 测试 → 一个 Git commit → 停止**

执行。

---

## 最重要的规则

请始终遵守：

> **PROJECT.md 是正式需求基线。**

> **源码是当前实现事实。**

> **只开发 Web。**

> **一次只处理一个功能。**

> **优先复用现有代码。**

> **测试登录时直接使用账号 `testuser` / 密码 `12345678`，禁止尝试走带验证码的注册流程。**

> **最小必要修改。**

> **完成后必须实际测试。**

> **每个功能单独 Git commit。**

> **Commit 描述使用中文，必要技术关键词保留英文。**

> **提交完成后立即停止，不要自动开始下一个功能。**
